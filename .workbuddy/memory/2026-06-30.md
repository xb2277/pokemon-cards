# 2026-06-30 工作日志

## 卡牌数据库页面改造

### 需求
1. 视图切换按钮（表格视图 / 卡片视图）常驻，不随视图容器一起隐藏
2. 去除表格和卡片中的"编辑"和"删除"按钮，数据库页面只读浏览
3. 新增"加入我的卡牌"交互：
   - 表格视图：操作栏显示"＋ 加入我的卡牌"按钮
   - 卡片视图：每张卡片右上角悬浮"＋"圆形按钮
   - 点击弹窗：显示卡牌预览 + 数量/成本价/品相/备注字段
   - 保存后调用 `createCardFromCatalog()` 写入 cards 表（已有则累加数量）

### 技术细节
- 视图切换按钮移到 `.toolbar` 区域（`margin-left:auto` 右对齐），不再嵌套在 `#catalogTableSection` 内
- 新增 CSS 类 `.catalog-card-add-btn`（红色圆形悬浮按钮，hover 时出现）
- 移除了 catalogModal（新增/编辑表单）、catalogDeleteModal（删除确认）
- 保留 bulkModal（批量导入），因管理员可能仍需使用
- `catalog.js` 全量重写，约 270 行

### 文件变更
- `catalog.html`：v15 → v16
- `assets/catalog.js`：v15 → v16（全量重写）
- `assets/style.css`：新增 `.catalog-card-add-btn` 样式

### 已推送
- commit: feat: catalog - view toggle always-on, remove edit/delete, add 'add to my cards' button
- 线上：https://xb2277.github.io/pokemon-cards/catalog.html

# 2026-06-27 工作日志

## GitHub Pages 部署完成

### 完成的工作
1. **catalog.html** — 创建静态版卡牌数据库页面，适配 Supabase SDK
2. **admin.html** — 创建静态版管理后台页面，包含用户列表、卡种管理 CRUD
3. **login.html** — 管理员登录后跳转 admin.html
4. **schema.sql** — profiles 表增加 email 字段，handle_new_user 触发器同步 email
5. **GitHub Pages 部署**
   - 创建 gh-pages 分支，仅包含静态站点文件
   - 添加 .nojekyll 禁用 Jekyll 处理
   - 通过 GitHub API 配置 Pages 源为 gh-pages 分支
   - 在线地址：https://xb2277.github.io/pokemon-cards/
6. **main 分支提交** — docs/ 下所有静态文件提交到 main 防止丢失

### 关键问题
- 本机代理端口 53848 已失效，正确端口为 7897
- git push 需指定代理：`git -c http.proxy=http://127.0.0.1:7897 push origin gh-pages`
- gh-pages 分支创建过程中 untracked 文件丢失，需从 gh-pages 分支 checkout 恢复

### 静态站点文件清单（docs/ 目录）
- index.html, dashboard.html, price.html, card_detail.html
- login.html, register.html, catalog.html, admin.html
- assets/: app.js, catalog.js, supabase-config.js, style.css, echarts.min.js
- supabase/: schema.sql
- .nojekyll

### schema.sql 修复（Supabase SQL Editor 兼容）
- 问题：`$$` 美元引用的函数体被 Supabase SQL Editor 语句分割器误判，报 `syntax error at end of input` LINE 0
- 修复：将所有 `$$` 改为 `$body$`，`CREATE OR REPLACE TRIGGER` 改为 `DROP TRIGGER IF EXISTS` + `CREATE TRIGGER`
- 已同步推送到 main 和 gh-pages 分支

### 用户待操作
1. 创建 Supabase 项目
2. 在 SQL Editor 运行 docs/supabase/schema.sql（已修复兼容性）
3. 填写 docs/assets/supabase-config.js 中的 SUPABASE_URL 和 SUPABASE_ANON_KEY
4. 运行 migrate_to_supabase.py 迁移数据
5. 将更新后的 supabase-config.js 推送到 gh-pages

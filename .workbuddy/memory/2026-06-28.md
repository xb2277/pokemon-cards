# 2026-06-28 工作日志

## GitHub Pages + Supabase 部署完成

### 已完成
- **序列修复**：运行 `setval` 同步 cards/price_records 序列，新增卡牌报错已解决
- **管理后台拆分**：admin.html（用户管理）+ admin-cards.html（卡牌管理），Tab 切换
- **RLS 策略验证与加固**：
  - cards 表：SELECT/INSERT/UPDATE/DELETE 全部限制 `auth.uid() = user_id OR is_admin()`
  - card_catalog 表：SELECT 全员可读，INSERT/UPDATE/DELETE 仅 admin
  - INSERT 策略从无限制（NULL）升级为 WITH CHECK 防止跨用户插入

### 数据状态
- card_catalog: 631 条（朱/紫系列 11 个扩展包，热门稀有度过滤）
- cards: 12 条（2 个用户）
- price_records: 72 条
- RLS 已启用且策略完整

### 线上状态
- 地址：https://xb2277.github.io/pokemon-cards/
- 架构确认：多人共享卡牌数据库（catalog）+ 各自个人卡牌仓库（cards）

---

## Pokemon TCG API 数据同步

### 已完成
- **sync_tcg_cards.py**：从 Pokemon TCG API 自动抓取卡牌数据并写入 Supabase
  - 支持按扩展包同步（sv1 ~ sv9）
  - 支持稀有度过滤（只拉热门卡）
  - 去重逻辑（基于 tcg_id）
  - 本地备份功能（backup_supabase.py）

### 备份
- 备份目录：`/Volumes/资料/workbuddy/kapai/backups/`
- 备份内容：card_catalog, cards, price_records, profiles, snapshots
- 备份格式：JSON（带时间戳 + latest 版本）

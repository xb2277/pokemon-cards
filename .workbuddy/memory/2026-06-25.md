# 2026-06-25

## 用户注册系统上线
- **数据库**：users 表新增 email 字段，`get_user_by_email()`, `bind_wechat_to_user()` 函数
- **注册 API**：`POST /api/auth/register` — email + username + password + confirm_password，含完整前端/后端验证
- **登录改造**：`POST /api/auth/web-login` 改为优先邮箱登录，保留用户名向后兼容
- **微信绑定 API**：`POST /api/auth/bind-wechat` — Web 用户可绑定小程序 openid
- **前端页面**：
  - 新建 `templates/register.html`（注册页，含实时字段验证）
  - 更新 `templates/login.html`（邮箱登录 + 注册链接）
  - 更新 `templates/admin.html`（用户列表增加邮箱列）
  - 更新 `static/app.js`（导航栏增加注册入口）
- **修复**：init_db 中 email 索引创建移到了 migration 之后，避免"no such column"错误
- **测试验证**：注册、邮箱登录、管理后台查看用户、微信绑定、输入验证全部通过

## 管理员账号
- admin / kapai2026（保持不变）

## 20:42 — 修复"退出"按钮无效

- **问题**：`app.js` 中动态生成的退出链接用 `innerHTML` + 内联 `onclick`，该方式在动态注入场景不可靠
- **修复**：
  1. 将内联 `onclick` 改为通过 `addEventListener` 绑定的 `click` 事件处理
  2. `localStorage.removeItem('token')` 改为 `localStorage.clear()`（与 admin.html 的 `doLogout()` 保持一致）
  3. 所有模板中 `app.js` 的缓存版本从 `v=3` 升至 `v=4`，确保浏览器加载新代码

## 20:49 — 修复视图切换"卡片视图"后按钮消失

- **问题**：`applyView('grid')` 隐藏整个 `#inventorySection`（含 `.section-header` 中的"表格视图/卡片视图"切换按钮），导致切换到卡片视图后无法切回
- **修复**：
  1. HTML 拆分：切换按钮所在的 `.section-header` 独立为 `#inventorySection`（始终可见），表格部分用新 `#inventoryTableWrapper` 包裹
  2. JS 更新：`applyView()` 改为 toggle `#inventoryTableWrapper` 而非 `#inventorySection`
  3. `renderCards()` 移除 section 显隐逻辑，保留 header 永远可见
  4. `#btnGridView` 补上 id 确保 `toggleView()` 能高亮
  5. 缓存版本 v4 → v5

## 20:55 — 修复价格页面自动创建卡牌到收藏

- **问题**：`selectPriceCatalogItem()` 调 `POST /api/cards/from-catalog`，后者通过 `ensure_card_from_catalog()` 在用户没有该卡时自动创建一张 quantity=1 的卡到收藏
- **修复**：
  1. 新增 `GET /api/catalog/<id>/card` API（仅查找不创建），返回 `{catalog, card|null}`
  2. `selectPriceCatalogItem()` 改用新 API
  3. 无 card 时显示提示"你还未添加此卡牌到收藏"，隐藏价格面板和手动录入
  4. 有 card 时正常展示价格历史
  5. 缓存版本 v5 → v6

## 21:00 — 小程序端同步修复：价格页面自动创建卡牌

- **排查结果**：
  1. 退出登录 Bug → 小程序不存在（使用 `bindtap` 而非内联 `onclick`）
  2. 视图切换 Bug → 小程序没有视图切换功能，不适用
  3. 价格页自动创建卡牌 → **存在完全相同的问题**
- **修复**：
  1. `utils/api.js`：新增 `catalogCardLookup()` 调用 `GET /api/catalog/<id>/card`（只读查询）
  2. `pages/price/price.js`：`selectCatalogCard()` 改用 `catalogCardLookup` 替代 `cardFromCatalog`，未收藏时显示提示而非自动创建
  3. `pages/price/price.wxml`：新增"未收藏"提示卡片
  4. 保留 `cardFromCatalog()`（收藏页添加卡牌仍需要使用）

## 21:06 — 继续修复：`ensureGengarPriceData` 仍在自动创建卡牌

- **根因**：上一次修复漏了 `ensureGengarPriceData()`（第 757 行），它在用户首次访问价格页时通过 `POST /api/cards/from-catalog` 自动创建耿鬼卡到收藏。价格页选择器没问题，是"耿鬼示例数据"种子逻辑在搞事。
- **修复**：`ensureGengarPriceData` 改用 `GET /api/catalog/<id>/card` 只读查询，用户没有该卡则跳过示例数据播种，不会自动创建卡牌。
- 缓存版本 v6 → v7

## 21:17 — 价格行情与库存完全解耦（catalog_id 驱动）

- **需求**：用户指出"库存与价格行情的数据应该无关，价格行情数据直接同步卡牌数据库中的数据"
- **架构变化**：
  - 价格记录从绑定 `cards.id`（用户库存）改为绑定 `card_catalog.id`（卡牌数据库，所有用户共享）
  - 任何用户都可以查看任意卡牌数据库中的卡牌价格，无需将其加入收藏
- **数据库层**：
  - `price_records` 表重建：`card_id` 改为 `DEFAULT NULL`（可空），`catalog_id` 作为主外键
  - 新增 `add_catalog_price_record()`、`get_catalog_prices()`、`get_catalog_latest_price()` ← 之前已加
  - 添加 WAL 模式 + busy_timeout 解决并发锁问题
- **后端 `app.py`**：
  - `GET /api/prices` 支持 `catalog_id` 参数
  - `POST /api/prices/manual` 支持 `catalog_id` 参数
  - 保留 `card_id` 兼容（旧数据）
- **前端 `app.js`**：
  - `selectPriceCatalogItem()` 去除"未收藏"限制，直接显示价格面板
  - `loadCardPrices()` → `loadCatalogPrices()`，用 `catalog_id`
  - `ensureGengarPriceData()` 直接用 `catalog_id` 播种
  - `refreshCardPrice()` 用 `currentCatalogId`
  - `submitManualPrice()` 用 `catalog_id`
  - `currentPriceCardId` → `currentCatalogId`
- **验证**：测试用户 0 张卡 → 查看价格 → 手动录入 → 卡片数为 0，没有自动创建
- 缓存 v7 → v8
